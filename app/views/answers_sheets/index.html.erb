<h1> exams </h1>
<div class="row">
	<div class="span6 offset3">
      <%= form_for @new_sheet, url: {action: "new"}, method: 'get' do |f| %>
        <%= f.collection_select(:subject_id, Subject.all, :id, :subject) %>
        <%= f.submit "take exam", class: "btn btn-primary" %>
      <% end %>
	</div>
</div>
<%= will_paginate %>
<ul>
  <%= render partial: "answers_sheet", collection: @answers_sheets %>
</ul>
<%= will_paginate %>